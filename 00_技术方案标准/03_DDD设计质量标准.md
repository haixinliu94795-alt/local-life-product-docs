# DDD 设计质量标准

## 聚合设计标准

每个聚合必须包含：

1. 聚合职责。
2. 聚合内实体和值对象。
3. 聚合不变量。
4. 可接受的 Command。
5. 产生的 Domain Event。
6. 不能跨聚合直接修改的字段。
7. 持久化边界。

## 限界上下文标准

限界上下文不是包名，而是语义边界。上下文之间只能通过应用服务、领域事件或防腐端口交互，不能共享内部实体。

## 反例

1. BFF 页面展示口径反向定义领域状态。
2. 旧系统表字段反向定义新平台领域对象。
3. 一个 Service 同时负责清分、结算、入账、查询展示。
