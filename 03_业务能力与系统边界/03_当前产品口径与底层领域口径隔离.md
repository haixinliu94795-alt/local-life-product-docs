# 当前产品口径与底层领域口径隔离

## 本章结论

当前产品把很多“核销后未结算”的金额统称为待结算，这属于展示口径。底层清结算平台必须使用标准状态：`IN_TRANSIT`、`ELIGIBLE`、`LOCKED`、`ACCOUNTING_PROCESSING`、`ACCOUNTED`。

## 隔离原则

1. 商户端想展示宽口径待结算，由 BFF 聚合，不进入底层状态机。
2. 后台商家应付必须只使用 `ELIGIBLE` 头寸。
3. 底层结算单状态只能代表资金链路真实进度。
4. 旧系统字段只作为迁移参考，不进入领域语言。
