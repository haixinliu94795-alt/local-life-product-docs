# 采用 SourceEvent -> ClearingResult -> SettlementPosition -> SettlementBill 主模型

## 状态

已接受。

## 背景

旧系统表和页面命名不能支撑平台化扩展。

## 决策

采用标准清结算领域模型。

## 影响

新业务通过事件和策略接入，不改主链路。

## 验证

对应设计必须能在领域模型、接口契约、状态机、DDL 和测试验收中找到落点。
