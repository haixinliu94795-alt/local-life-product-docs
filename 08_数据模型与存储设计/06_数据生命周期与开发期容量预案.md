# 数据生命周期与开发期容量预案

## 本章结论

本版不输出上线部署方案，但开发阶段必须考虑大数据量下的表增长、扫描任务和归档边界。

## 设计要求

1. 账期成熟任务必须按 `lifecycle_status + eligible_time + id` 分页推进。
2. 批量结算必须设置单批头寸数量上限。
3. 查询必须按商户、业务线、状态、时间范围过滤。
4. P0 不实现历史归档，但必须预留按时间归档的查询边界。
5. 操作日志不可被业务更新覆盖，只能追加。
