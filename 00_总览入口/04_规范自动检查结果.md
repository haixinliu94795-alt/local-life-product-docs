# 规范自动检查结果

本检查只针对 V005 P0 的 DDL 和核心实现约束。说明性文档中为了响应评审意见，可能会出现“删除旧映射”“不绑定旧方法”等描述，不视为设计违规。

| 检查项 | 结果 | 说明 |
|---|---|---|
| DDL 金额是否使用 BIGINT 分 | 通过 | 未发现 `DECIMAL`。 |
| DDL 是否设置币种列 | 通过 | 未发现 `currency / currency_code / cur` 字段。 |
| DDL 是否设置租户列 | 通过 | 未发现 `tenant_id` 字段。 |
| DDL 是否设置退款列 | 通过 | 未发现 `refund_no / refund_amount / source_refund_no` 字段。 |
| DDL 是否设置旧系统映射列 | 通过 | 未发现旧 `finance_detail` 映射列。 |
| DDL 是否存在 `ccs_settlement_position` | 通过 | 已用结算头寸模型替代产品口径待结算表。 |
| 核心接口是否使用标准账务端口 | 通过 | `SettlementAccountingPort` 为设计契约。 |
| 核心状态机是否包含 UNKNOWN 补偿 | 通过 | 头寸、结算单、账务入账单均有 UNKNOWN 路径。 |
