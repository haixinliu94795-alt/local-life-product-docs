# 幂等并发测试

| 用例 | 预期 |
|---|---|
| 同一来源事件重复提交，hash 相同 | 返回同一 `event_no`。 |
| 同一来源事件重复提交，hash 不同 | `CCS_IDEMPOTENT_CONFLICT`。 |
| 同一结算确认重复提交，hash 相同 | 返回同一 `bill_no`。 |
| 同一结算确认重复提交，hash 不同 | `CCS_IDEMPOTENT_CONFLICT`。 |
| 两个请求同时锁定同一头寸 | 只有一个成功。 |
| 批量中混入已锁定头寸 | 整批失败。 |
| 批量中混入非同一商户头寸 | 整批失败。 |
