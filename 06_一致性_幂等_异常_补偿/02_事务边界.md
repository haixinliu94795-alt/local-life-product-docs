# 事务边界

## 1. 确认结算事务

事务内完成：

1. 幂等检查。
2. 查询并锁定 `ELIGIBLE` 待结算项。
3. 校验商户、结算主体、业务场景、金额。
4. 生成批次、结算单、结算明细、入账单。
5. 待结算项推进到 `LOCKED`。
6. 写操作日志。

事务外或第二阶段完成：

1. 调用账户账务平台。
2. 根据结果推进状态。

## 2. 为什么不在一个事务内包住账务调用

账户账务平台是外部系统。跨系统调用不应长期占用本地数据库事务。P0 可以同步调用，但调用前必须先有本地入账单和可追踪状态。
