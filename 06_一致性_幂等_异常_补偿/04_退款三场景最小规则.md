# 退款三场景最小规则

| 场景 | 状态 | P0 处理 |
|---|---|---|
| 结算前退款 | `IN_TRANSIT` / `ELIGIBLE` | 全额退款取消，部分退款调减。 |
| 结算中退款 | `LOCKED` / `ACCOUNTING_PROCESSING` | 退款事件等待，不直接打断结算。 |
| 结算后退款 | `ACCOUNTED` | 生成负向清分和负向待结算项，下一期抵扣。 |

## 约束

- 不修改历史已成功结算单金额。
- 不对已入账成功单直接做删除或回退。
- 负向调整必须能追溯到原订单、原结算单和退款单。
