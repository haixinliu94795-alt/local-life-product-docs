# 回滚与人工处理预案

## 回滚策略

1. 关闭新事件接入。
2. 新核销重新走旧链路。
3. 已经生成但未入账的结算单暂停处理。
4. `ACCOUNTING_PROCESSING` 和 `UNKNOWN` 单据必须先人工核查，不能直接删除。

## 人工处理

| 状态 | 处理 |
|---|---|
| `ACCOUNTING_FAILED` | 可重试或取消后释放。 |
| `UNKNOWN` | 查询账务平台后回正。 |
| `LOCKED` 长时间未入账 | 检查关联结算单和入账单。 |
| 商户端口径异常 | 对比新老双轨数据来源。 |
